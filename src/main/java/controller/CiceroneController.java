package controller;

/**
 * Rappresenta un controller per un utente <code>Cicerone</code> che elabora le sue interazioni con il sistema.
 */
public interface CiceroneController {

    /**
     * Permette di elaborare la creazione di una nuova <code>Esperienza</code>.
     */
    void creaNuovaEsperienza();
}
